你应该在本目录创建一个Kt文件放置你的Api Keys，如：

```
package me.earzuchan.chatdrama.framework.llm

const val VANYO_OPENAI_KEY = ...
const val DEEPSEEK_KEY = ...
const val OPUSRELAY_KEY = ...
const val GEMINI_KEY = ...
```

需要测试你自己的端点，就去改[这个文件](IntegrationTest.kt)，值得注意的是：有一些端点不支持跨域，那在浏览器平台里跑不通。